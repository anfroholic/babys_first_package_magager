# Baby's first package manager

To begin

```bash
docker compose up
```

Then open `http://localhost:8000` in your browser

`Load Parameters` button will populate packages from example parameters. 

`Replay` will replay some of the saved actions in replay.json